module.exports = function (oldFile, tempFile, duckProperties) {
  const { duckName, actionName, actionCreatorName, defaultStateName, defaultStateValue } = duckProperties

  console.log('duck properties:', duckProperties)
}